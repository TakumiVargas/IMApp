<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mcm.edu.ph.vargas_imapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vargas_iMApp">
        <activity android:name=".PMScreen"></activity>
        <activity android:name=".LoginScreen"></activity>
        <activity android:name=".SplashScreen" ></activity>
        <activity android:name=".GalleryScreen" ></activity>
        <activity android:name=".Registerscreen" ></activity>
        <activity android:name=".MainScreen">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
    </application>

</manifest>